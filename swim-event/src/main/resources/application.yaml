spring:
   datasource:
      username: swim_event
      password: swim_event
      url: jdbc:mysql://localhost:3306/swim_event

   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true
      
   sql:
     init:
       mode: never